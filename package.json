{
  "name": "kaiapp",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "test:convex": "node scripts/test-convex-connection.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "convex": "^1.26.1",
    "dotenv": "^16.6.1",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
